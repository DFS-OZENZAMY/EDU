import { SuperAdminLayout } from "@/components/super-admin/super-layout"

export default function SuperLayout({ children }: { children: React.ReactNode }) {
  return <SuperAdminLayout>{children}</SuperAdminLayout>
}
